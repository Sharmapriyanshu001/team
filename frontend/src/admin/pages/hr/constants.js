/**
 * The HR vocabulary moved to shared/hr when the HR panel started using the
 * same screens. Re-exported here so anything that imported it from the admin
 * pages keeps working, and so there is still exactly one copy of the lists.
 */
export * from "../../../shared/hr/constants";
