import FileManager from "../files/FileManager";

export default function ClientDocuments() {
  return (
    <FileManager
      title="Client Documents"
      subtitle="Contracts, invoices and drawings filed against each client"
    />
  );
}
