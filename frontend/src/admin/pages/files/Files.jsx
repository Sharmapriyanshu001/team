import FileManager from "./FileManager";

export default function Files() {
  return (
    <FileManager
      title="Files"
      subtitle="Every document stored across clients and projects"
      showAssignTask
    />
  );
}
